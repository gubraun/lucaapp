
public struct BackendCredentials {
    let login: String
    let password: String
}

public let backendCreds = BackendCredentials(
    login: "{{ argument.login }}".removingPercentEncoding ?? "",
    password: "{{ argument.password }}".removingPercentEncoding ?? "")
